#include<bits/stdc++.h>
using namespace std;
{
    int n,mx=-1;
    scanf("%d",&n);
    map<string,int> mp;
    vector<string> v(n);
    for(int i=0;i<n;i++){
        cin>>v;
        mp[v]++;
    }
    for(int i=0;i<mp.size()){
        mx=
    }
}