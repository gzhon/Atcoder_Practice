#include<bits/stdc++.h>
using namespace std;
int main(void)
{
    int n,one=0,two=0;
    cin>>n;
    vector<int> a(n),b(n);
    vector<bool> v(n);
    for(int i=0;i<n;i++) cin>>a[i];
    for(int i=0;i<n;i++) cin>>b[i];
    for(int i=0;i<n;i++){
        if()
    }
    return 0;
}