#include<bits/stdc++.h>
using namespace std;
int main(void)
{
    string s,p,ans;
    long long dig=0;
    scanf("%lld %lld",&a,&b);
    while(p.size<dig){
        if(stoll(s[dig])+stoll(p[dig])>=10){
            puts("Hard");
        }
        dig++;
    }
    puts("Easy");
    return 0;
}