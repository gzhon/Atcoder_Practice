#include<bits/stdc++.h>
using namespace std;
int main(void)
{
    int n,m,ans=0;
    cin>>n>>m;
    vector<int> u(n),v(n);
    for(int i=0;i<n;i++) cin>>u[i]>>v[i];
    for(int i=0;i<n;i++){
        
    }
    return 0;
}